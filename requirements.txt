# Nephodex — Hugging Face Gradio Space dependencies.
# Canonical dependency list (the Space builds from this file).
# Local dev: `uv venv && uv pip install -r requirements.txt`
#
# gradio is provided by the Space's `sdk_version` (see README: 5.38.2) — NOT
# pinned here, to avoid fighting the SDK's own resolution. This set was resolved
# with `uv pip compile` and verified: transformers 4.51.3 loads the SmolVLM2
# processor (5.x regressed it); gradio 5.38 fixes the 4.44 runtime bugs while
# still allowing huggingface_hub<1.0, which transformers 4.51.3 requires.

transformers==4.51.3
huggingface_hub==0.30.2
accelerate==1.13.0
num2words               # SmolVLM2 processor dependency on some transformers versions

torch                   # Space provides the matching CUDA build
opencv-python-headless  # headless: no GUI libs, correct for a Space
numpy
pillow
scikit-learn            # cosine_similarity for the "seen this before?" check
